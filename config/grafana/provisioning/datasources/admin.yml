# GENERATED by scripts/generate.py from clients.yml — do not edit by hand.
apiVersion: 1

deleteDatasources:
  - name: Prometheus
    orgId: 1
  - name: Loki
    orgId: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 30s
      httpMethod: POST

  - name: Loki
    uid: loki
    type: loki
    access: proxy
    orgId: 1
    url: http://loki:3100
    editable: false
    jsonData:
      timeout: 60
      maxLines: 5000
      # Every tenant at once — admin view.
      httpHeaderName1: X-Scope-OrgID
    secureJsonData:
      httpHeaderValue1: "self"
